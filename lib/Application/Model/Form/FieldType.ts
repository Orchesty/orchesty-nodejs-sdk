enum FieldType {
    TEXT = 'text',
    NUMBER = 'number',
    URL = 'url',
    PASSWORD = 'password',
    SELECT_BOX = 'selectbox',
    MULTI_SELECT = 'multiselect',
    CHECKBOX = 'checkbox',
}

export default FieldType;
